// Package api implements ComicHero's HTTP operations and application use cases.
// Files are grouped by product feature and, for larger features, by route,
// query, detail, mutation, and entry responsibilities.
package api
